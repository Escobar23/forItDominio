{
  "extends": "../tsconfig.json",
  "name": "domain",
  "version": "1.0.0",
  "main": "index.js",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": [
    "src"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^latest",
    "@types/node": "^latest"
  }
}
