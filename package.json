{
  "name": "dominioproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
        "lint": "eslint . --fix --report-unused-disable-directives",
        "format": "prettier --write .",
        "fe": "yarn workspaces foreach -vvpA --exclude '.'",
        "fet": "yarn fe --topological",
        "test": "yarn fe --exclude 'packages/templates/*' run test --run --clearScreen false",
        "build": "yarn workspace domain build"
    },
   "private": true,
  "workspaces": [
    "domain",
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
